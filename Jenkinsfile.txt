pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                dir('backend/ASAF/src/main/java/com/ASAF') { // 프로젝트 하위 디렉터리로 이동
                    sh './gradlew build'
                }
            }
        }

        // 추가 단계 (테스트, 배포 등)
    }
}
